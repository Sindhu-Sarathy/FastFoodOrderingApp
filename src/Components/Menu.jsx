import React from 'react';

const Menu = ({items,addToOrder}) => {
    return (
        <div>
            <h2 className='text-center'>Fast Food Menu</h2>
            <div class></div>
        </div>
    );
};

export default Menu;